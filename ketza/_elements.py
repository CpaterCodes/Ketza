
from .tags import tag

# Set of predefined elements, based on the tag function

a = tag('a')
abbr = tag('abbr')
acronym = tag('acronym')
address = tag('address')
applet = tag('applet') 
area = tag('area')
article = tag('article')
aside = tag('aside')
audio = tag('audio')

b = tag('b')
base = tag('base')
basefont = tag('basefont')
bdi = tag('bdi')
bdo = tag('bdo')
big = tag('big')
blockquote = tag('blockquote')
body = tag('body')
br = tag('br')
button = tag('button')

canvas = tag('canvas')
caption = tag('caption')
center = tag('center')
cite = tag('cite')
code = tag('code')
col = tag('col')
colgroup = tag('colgroup')

data = tag('data')
datalist = tag('datalist')
dd = tag('dd')
deleted = tag('del') # More verbose, but del is already reserved in python
details = tag('details')
dfn = tag('dfn')
dialog = tag('dialog')
dir = tag('dir')
div = tag('div')
dl = tag('dl')
dt = tag('dt')

h1 = tag('h1')

li = tag('li')

main = tag('main')

p = tag('p')

section = tag('section')

ul = tag('ul')

